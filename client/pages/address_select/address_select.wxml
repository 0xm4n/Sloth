<view class="container">
  <radio-group bindchange="radioChange" class="radio-group">
    <label wx:for="{{cartArr}}">
      <view class='each_row' style="flex-direction:row;">
        <view class="label" style="flex-direction:column;">
          <view class="decom">{{item.decom}}</view>
          <view class="personal_info" style="flex-direction:row;">
            <view class="name">{{item.name }} </view>
            <view class="phone"> {{item.phone}}</view>
          </view>
        </view>
        <view>
          <navigator url='../address_alter/address_alter?userId={{userId}}&cartArr={{cartArr}}'>
            <image class='icon_change' src='./writing.png'></image>
          </navigator>
        </view>
      </view>

    </label>
  </radio-group>
  <view class="button_new_address">
    <navigator url='../address_add/address_add?userId={{userId+1}}'>新增地址</navigator>
  </view>

</view>